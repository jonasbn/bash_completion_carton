_carton() {
    cur="${COMP_WORDS[COMP_CWORD]}"
	commands="bundle exec help install"
	
    if [ $COMP_CWORD == 1 ]
    then
      COMPREPLY=($(compgen -W "${commands}" -- ${cur}))
      return 0
    fi
}

complete -F _carton carton